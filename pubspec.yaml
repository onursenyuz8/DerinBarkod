name: derin_barkod
environment:
  sdk: ">=3.3.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  intl: ^0.19.0
  image_picker: ^1.1.2   # (foto ekleme için; sonra kullanırız)
